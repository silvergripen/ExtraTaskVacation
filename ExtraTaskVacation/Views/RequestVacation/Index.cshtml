@model IEnumerable<VacationTaskUppgift.Models.RequestVacationModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DateStart)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateEnd)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VacationType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FK_Personel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateStart)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateEnd)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VacationType.TypeId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FK_Personel)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RequestVacId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RequestVacId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RequestVacId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
